<% if current_profile.liked? @post %>
	$(".like-btn" + <%= @post.id %>).addClass("liked");
<% else %>
	$(".like-btn" + <%= @post.id %>).removeClass("liked");
<% end %>
$(".likes-count-" + <%= @post.id %>).html("<%=@post.get_upvotes.size%> <%="like".pluralize(@post.get_upvotes.size)%>");
