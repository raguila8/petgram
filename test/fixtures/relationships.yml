# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This queries the database for rows instantiated in profile fixture
one:
  follower_id: <%=Profile.find(ActiveRecord::FixtureSet.identify(:one)).id%>
  followed_id: <%=Profile.find(ActiveRecord::FixtureSet.identify(:two)).id%>


two: 
  follower_id: <%=Profile.find(ActiveRecord::FixtureSet.identify(:two)).id%>
  followed_id: <%=Profile.find(ActiveRecord::FixtureSet.identify(:one)).id%>

